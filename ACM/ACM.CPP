#include <bits/stdc++.h>
using namespace std;
void doc()
{
    freopen("ACM.INP","r",stdin);
    freopen("ACM.OUT","w",stdout);
}
int main()
{
    //doc();
    cin>>n;
    for (i=1;i<=n;i++) cin>>a[i];
    f[1]=0;
    for (i=2;i<=n;i++)
    {
        for (j=i;j>=1;j--)
        {
            if (a[i]> a[j])
        }
    }
}
