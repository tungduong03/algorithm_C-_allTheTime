#include <bits/stdc++.h>
using namespace std;
int main()
typedef struct customer
{
	int y;
	bool z;
}TY_Customer;
{
    freopen("DANCE.INP","r",stdin);
    freopen("DANCE.OUT","w",stdout);

    int ;
    cin>>n>>k;
    bool check[100003];

    for (int i=1; i<=n; i++)
    {
        int x;
        cin>>x;
        if (x>0) arr[i]=x,check[i]=true;
        else arr[i]=-x,check[i]=false;
    }




}
